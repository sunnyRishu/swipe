// Defining the groups as they belong to some inventory
// It's dynamic like if we add more items to groups it will reflect well in the frontend and it's functionality
const groups = [
    "Labor",
    "Materials"
];

export default groups;